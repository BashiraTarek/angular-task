import { ExchangeratePipe } from './exchangerate.pipe';

describe('ExchangeratePipe', () => {
  it('create an instance', () => {
    const pipe = new ExchangeratePipe();
    expect(pipe).toBeTruthy();
  });
});
